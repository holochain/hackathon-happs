[package]
name = "feedback"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
name = "feedback"

[dependencies]
hdk = { workspace = true }

serde = { workspace = true }

feedback_integrity = { workspace = true } 
